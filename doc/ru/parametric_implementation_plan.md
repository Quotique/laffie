## План реализации параметрического каталога (Phase 1)

Артефакт Phase 0 — каталог из 12 параметрических карточек, типология
приёмов, сетка покрытия и outline статьи в
[`parametric_catalog.md`](parametric_catalog.md).

Цель Phase 1 — рабочая система в laffie + черновик статьи с разбором 1–2
эталонных кейсов.

Базовый паттерн реализации: rule-based laffie + PyO3-bridge к SymPy через
procedural symbol с `py "..."` блоком и declarative rule с `||` в
резолюции. Точное состояние bridge-инфраструктуры устанавливается на
шаге 1.

Список задач в порядке очерёдности; распределение по времени не
фиксируется.

### Сквозные обязательства

Применяются ко всем задачам, меняющим код:

- Обновлять `claude_docs/` при добавлении / переименовании / удалении
  файлов, типов, public API (правила репозитория, см. `CLAUDE.md`).
- Тесты: unit-уровень — `cargo test --workspace`; нестабильные текстовые
  выводы — snapshot через `insta`; интеграционный уровень — `.pbl`-задачи
  в `tasks/sympy_comparison/parametric/`, прогоняемые в составе
  `cargo test`.
- При завершении задачи прогонять `cargo clippy --workspace` и
  `cargo +nightly fmt` (правила `CLAUDE.md`).

### Подготовка инфраструктуры

1. **Архитектурное расследование текущего laffie.**
   - PyO3-bridge: `src/parser/symbol.rs`, файлы `symbols/py/*.py`.
     Запускается ли SymPy embed-режимом, какие term ↔ SymPy конверсии
     уже работают.
   - Эталонные procedural-symbol файлы: `symbols/poly_div.sym`,
     `doc/ru/rational_roots.md` (если упомянутый паттерн актуален).
   - Текущее представление ответа в solver (одиночный термин / set /
     дизъюнкция через `||`).
   - Синтаксис `find(x)` для задач со свободным параметром — как
     параметр объявляется в дано / условиях.
   - Находки зафиксировать в новом разделе `## Архитектурный обзор`
     этого файла либо в отдельном `doc/ru/architecture_state.md`.
   - **Возможный re-plan.** Если архитектура существенно отличается от
     ожидаемого паттерна (procedural symbol + decl rule с `||`), задачи
     2–12 требуют ревизии до начала реализации.

2. **Представление `x ∈ ℝ` в ответе (инфраструктура).**
   Сейчас не выражается. Ввести минимальный механизм: символ `Reals`,
   правило `Reals is known <=> true`, и убедиться, что answer-checker
   принимает форму `x in Reals`. Sanity-check `.pbl` — задача с
   `x in Reals` как фактом, проверяющая только инфраструктуру
   распознавания.

   Триггер для генерации `x in Reals` из реальной задачи (например, из
   тождества `0·x = 0`) сюда **не входит** — generic правило вида
   `a == a => x in Reals` слишком широкое и срабатывает на любой
   тавтологии в ходе подстановок. Производство `x in Reals` живёт
   внутри case-split правила (см. task 5), где конкретная ветвь
   `p == 0 && q == 0` явно эмитирует этот ответ.

3. **Обработка пустой ветви в дизъюнкции.**
   Эмпирическая проверка показала, что существующая инфраструктура
   `logic_and` + `logic_or` корректно сворачивает форму case-split вида
   `(cond_1 && answer_1) || (cond_2 && answer_2) || ...` когда условия
   `cond_i` конкретные (доказуемые/опровергаемые). Никаких новых правил
   добавлять не пришлось. Контрольные задачи `tasks/test/empty_branch.pbl`
   с тремя ветвями (одна — `x in empty_set`) фиксируют это.

   Бара top-level `x in empty_set || x == 7` (без условной обёртки)
   автоматически не сворачивается — но такая форма не возникает в
   реальном case-split, поэтому добавлять для неё правило не требуется
   (попытка добавить unconditional drop сломала existing test
   `tasks/test/irrational.pbl`, который полагается на сохранение
   empty_set как fallback в дизъюнкции с условной первой ветвью).

   **Открытая подзадача**, всплывающая на task 5: параметрический
   case-split вида `(a == 0 && x in Reals) || (a != 0 && x == 1)` —
   условие не может быть доказано/опровергнуто, дизъюнкция остаётся
   кусочным ответом. `check_find_answer` (`src/solver/task/solver.rs:588`)
   эту форму как валидный ответ не принимает (ожидает `x == ...` или
   `x in ...` напрямую). Закрывается в рамках task 5.

### Реализация эталонной карточки

4. **Procedural symbol `sympy_solve(eq, x)`.**
   Минимальный wrapper над `sympy.solve` для конкретного уравнения и
   неизвестной. Расширение `symbols/py/sympy_convert.py` для нужных
   операторов (минимум `==`). Тест unit-уровня: на конкретных уравнениях
   (`x² − 4 = 0` → `±2`) через bridge возвращаются ожидаемые term-ы.

5. **C1 end-to-end.** Закрыто.
   - Задача — `tasks/sympy_comparison/parametric/c1.pbl`. В givens, помимо
     уравнения `a·x = a`, объявлено `a is known;` — это стандартный
     способ пометить свободную переменную как параметр (через `a` можно
     выражать ответ). Без этого факта case-split rule с requirement
     `a is known` не активируется.
   - Правило — в `symbols/equal.sym` рядом с базовым `x*a + b == 0`:
     `attr level(2),goal(find(x, ..))`, шаблон `x*a + b == 0`,
     резолюция в обёртке `answer(...)` с тремя ветвями
     `(a == 0 && b == 0 && x ∈ ℝ) || (a == 0 && b ≠ 0 && x ∈ ∅) ||
     (a ≠ 0 && x ∈ sympy_solve(x*a + b == 0, x))`. Requirements —
     `a is known, b is known` (без них рекурсия — см. ниже).
   - **Подзадача 5а — закрылась без изменений в коде.**
     `answer(...)` обёртка в резолюции + существующая инфраструктура
     `logic_and` / `logic_or` нормализуют дизъюнкцию конъюнкций до формы,
     которую принимает `check_find_answer`. Расширять answer-checker
     не пришлось — параметрический piecewise сравнивается структурно
     через `validate_answer`, ожидаемые формы перечисляются в `answer`.
   - **Подводный камень — рекурсия правила на собственный output.**
     Резолюция содержит `sympy_solve(x*a + b == 0, x)`, т.е. инкапсулирует
     LHS-паттерн. Без requirements правило срабатывает на этом внутреннем
     `x*a + b == 0` и даёт infinite loop через nested `answer(answer(...))`.
     Лечится requirement-ами `a is known, b is known`: они не доказуются
     для свободного параметра без givens, поэтому повторный re-fire
     невозможен. Givens-обязательство `a is known` — единая точка контроля
     на стороне постановки задачи.
   - Контрольный прогон — laffie выдаёт `a=0 ∧ x∈ℝ | a≠0 ∧ x∈set(1)`,
     совпадает с одной из форм `answer` в `.pbl`. Регрессий на полном
     наборе `tasks/test/` нет.

### Реализация case-split-семейства

6. **Следующие case-split карточки.** Закрыто.
   - **C24** — два независимых вырождения, минимальное расширение C1.
     Решается канонически за 1886 циклов.
   - **C2** — двухуровневый разбор. Канонический ответ за 2708 циклов.
     Закрытие потребовало: упрощения квадратного case-split rule
     (двухветочный split с делегированием линейной ветви через B3
     sub-solve), фикса divide-by-zero в `impl_divide`, расширения
     кэша на B3-подзадачи (ключ — полный `solve(...)` term).
   - `.pbl`-задачи в `tasks/sympy_comparison/parametric/`, прогоняются
     при `cargo test`.
   - Кандидаты на расширение в task 10: C26, C10.

### Реализация постпроцессинга

7. **Постпроцессинговый примитив.**
   Перед карточкой нужен общий механизм; выбор зависит от того, какая
   карточка выбрана в шаге 8:
   - Для **C29** — механизм «формальное решение SymPy → постфильтр
     предикатом» (приём ОДЗ-фильтра).
   - Для **C28** — CAS-минимум на piecewise-функции (через
     `sympy.minimum` или эквивалент). Дополнительно — механизм
     знак-разбора модулей в кусочно-линейную форму.
   
   Это самостоятельная инфраструктурная задача, отдельная от карточки.
   Тест unit-уровня на корректность примитива.

8. **Постпроцессинговая карточка.**
   Реализация выбранной карточки (**C29** или **C28**) поверх примитива
   из шага 7. Аналогично шагу 5 — `.pbl`-задача + интеграционный тест.

### Завершение

9. **Выбор showcase для статьи.**
   Из реализованного к моменту finalization выбираются 1–2 наиболее
   показательные карточки. Минимум: одна case-split (C1, C24 или C2) и
   одна постпроцессинговая (C29 или C28). Критерий — компактность +
   видимость всех трёх стадий гибрида. Решение оформляется записью в
   плане (либо отдельным разделом в `parametric_catalog.md`).

10. **Опциональные дополнительные карточки.**
    Если есть желание расширить набор реализованного: остальные карточки
    из {C2, C10, C25, C26}, не сделанные в шаге 6. Системы (C11, C21,
    C27) и предел C23 в первой итерации откладываются — требуют
    расширения инфраструктуры за рамки одиночного уравнения / алгебры.

11. **Текст статьи по outline.**
    Outline готов в `parametric_catalog.md` (раздел «Outline статьи»).
    Разделы 4 (типология приёмов) и 6 (покрытие) — материал переносится
    как есть. Разделы 1, 2, 3, 8, 9 — писать. Разделы 5 (каталог
    примеров) и 7 (реализация в laffie) — расширить выбранный showcase
    до полного разбора с фрагментами `.sym` / `.pbl` кода.

12. **Подготовка repo к публикации.**
    - `verify_parametric.py` в `doc/sympy_audit/` — regression-suite на
      реализованные карточки в едином формате.
    - Проверить README, `CLAUDE.md`, инструкцию запуска.
    - Tag / release с зафиксированной версией для статьи.

---

## Архитектурный обзор (после task 1)

Расследование завершено. Базовый паттерн памяти подтверждён, re-plan
задач 2–12 не требуется. Ниже — что подтверждено и какие точки остались
открытыми.

### Подтверждено

1. **PyO3-bridge активен** — `src/parser/symbol.rs:42–93` использует
   `Python::initialize()` + `Python::attach()` (embed, не subprocess).
   Term ↔ Python через JSON-сериализацию `TermBuf`. Калькулятор в Python
   получает обёрнутый `Term` и `level`-строку, возвращает `None`
   (без изменений) или новый `Term`.

2. **Procedural symbol pattern работает.** Эталон —
   `symbols/poly_div.sym`. Структура:
   ```
   symbol N {
       py "
   exec(open('symbols/py/sympy_convert.py').read())
   def calculator(root, level):
       ...
   ";
   }
   ```

3. **Конвертер `symbols/py/sympy_convert.py` существует** с функциями
   `to_sympy(node, var_map)` и `from_sympy(expr, var_name, Term)`.
   Поддерживаются операторы: `+`, `*`, `^`, `/`, `neg`. **Не
   поддерживаются:** `==`, `!=`, `<`, `>`, `<=`, `>=`, `Abs`, sqrt
   (отдельный символ), тригонометрия, логарифмы.

4. **Дизъюнкция `||` в резолюции правила работает.** Эталон —
   `symbols/power.sym` (теорема о рациональных корнях):
   `F == 0 => (x == u) || (Q == 0)`. Документировано в
   `doc/ru/rational_roots.md`.

5. **Grounding** через `Hypothesis::ground()`
   (`solver/rule/hypothesis.rs:48`) — equality binding + cartesian
   product по generator-ам `param in set(...)`.

6. **Goal types:** `find(x)`, `find(x, y, ...)`, `prove(p)`,
   `transform(t)`. Multi-var find собирает конъюнкцию ответа
   (`x == 3 && y == -2`).

7. **Формат ответа в `.pbl`:** `answer X, Y;` — список альтернативных
   форм. Решатель принимает любую совпавшую.

8. **Параметр в задаче — свободная переменная.** Подтверждено
   пользователем; в коде это `Param` (отличный от `Variable`,
   которая — то, что ищем). Param появляется через unification правил.

### Неподтверждено / открытые точки

A. **Отсутствует представление `x ∈ ℝ`.** Существующий `set` (в
   `symbols/set.sym`) поддерживает только конечные множества (`set(a)`,
   `set(a, b)`, `set(a, b, ..)`). Нет символа `Reals` или эквивалента.
   Минимум — ввести нульарный символ (например, `R` или `Reals`) и
   правило-нормализацию «`x in R` — валидный ответ для `find(x)`».

B. **Условный ответ от параметра** — самый существенный риск.
   Текущие тесты имеют безусловный ответ (`x == 3`) или набор альтернатив
   (`x == -1 || x == 1`). Семантика «ответ зависит от значения
   параметра», вида `(a == 0 && x in R) || (a != 0 && x == 1)`,
   не зафиксирована в существующих задачах; принимает ли её
   `check_find_answer` (`solver/task/solver.rs:588`) — нужно проверить
   эмпирически на task 5. Если нет — потребуется доработка либо
   альтернативный формат (отдельный piecewise-символ).

C. **Поведение `||` с false-ветвью.** Резолюция вида
   `(0 == 4) || (x == 1)` должна сворачиваться к `x == 1`. Базовый
   `false` явно не определён (`logic_true.sym` есть, `false`
   реализуется через `!true`). Автоматический отсев противоречивой
   ветви не проверен — задача 3.

D. **Дизъюнкция в multi-var find** — известный TODO в
   `tasks/test/equation.pbl`: «дизъюнктивный ответ в multi-var find».
   Не критично для одиночных уравнений (наш основной фокус), но
   блокер для систем (C11, C21, C27) — поэтому они в task 10
   откладываются.

E. **Параметрический case-split в существующих правилах не реализован.**
   В `symbols/equal.sym` есть базовое правило
   `x*a + b == 0 => x == -b/a` с requirement `a != 0`. Этот
   requirement блокирует применение правила для параметрического `a` с
   неизвестным знаком. Нужно ввести альтернативное правило с
   дизъюнктивной резолюцией, явно вводящее ветви по обнулению `a`.

### Влияние на дальнейшие задачи

- **Task 2 (`x ∈ ℝ`)** — нужен новый zero-arity символ + рулевая
  поддержка для answer-checking. Не блокирует, но требует
  проектирования.
- **Task 3 (пустая ветвь)** — расширяется до проверки автоматического
  отсева false-веток в `||`. Возможно, потребуется правило-нормализация
  или модификация в `check_find_answer`.
- **Task 5 (C1)** — план остаётся, но на этом шаге проверяется открытый
  пункт B. Если решатель не принимает кусочный ответ как валидный,
  task 5 расширяется до доработки answer-checker.
- **Task 6, 7, 8** — не блокируются текущим состоянием, но реалистичны
  после закрытия 2 и 3.

Re-plan не запускается: существенных архитектурных сюрпризов нет.

---

## Архитектурный беклог

Накапливающиеся архитектурные пункты, всплывающие в ходе реализации.
Не блокируют Phase 1 кейсы, но требуют осознанного решения после
завершения карточек.

### B1. Жадность `transform` — все правила сразу

`transform(...)` применяет к терму все подходящие правила без приоритета
/ ограничения. Конкретный симптом — на task 6 (C24): на старте
`x·(a²−4) = a−2` до того, как case-split rule успевает посмотреть на
факторизованный коэффициент, `brackets_disclosure` распаковывает
`x·(a²−4)` в `a²·x − 4·x`. Это, в свою очередь, гонит ping-pong с
`a*x^n + b*x^n + c <=> (a+b)*x^n + c` — collect свернул, transform
тут же распаковал.

**Возможные направления:** ограничение порядка применения правил
внутри transform (приоритеты, level-ordering, либо «не применять
правило, если оно отменяет предыдущий ход»), либо отдельный канал
«структурно-чувствительных» правил, которые видят терм до распаковки.

**Когда возвращаться:** после реализации task 6–8 — если симптомы
повторяются на других карточках. Сейчас обходится локально (см. task 6
fix про передачу `blocked_rules` в subtask).

**Частичный фикс — guard `c is atom` на brackets_disclosure.**
Reproducer `(p-1)*x^2 + q*x + r == 0` теперь решается канонически
(1765 циклов) — guard блокирует раскрытие compound-коэффициента,
который содержит find-target в степени. Добавлен truth-checker
`is atom` (true для leaf-атомов: Number, Variable, Param).

**C2 закрыт.** Понадобились ещё фиксы: (а) `impl_divide` возвращает
`None` на нулевом знаменателе (раньше крашился на `-1/(2·1-2)` при
substitute); (б) **B3 step 3 — финальная итерация через cache**:
`run_solve_block` использует полный `solve(find(vars), eqs...)` term
как cache-ключ. Placeholder solution вставляется до `solve_impl` —
рекурсивный вызов на ту же форму попадает в empty marker и дропается;
другая форма (меньший многочлен и т.п.) даёт новый ключ и legitimate
recursion работает. Преимущество над `blocked_rules`-вариантом: не
блокирует правило в случаях, где оно ДОЛЖНО рекурсировать (например,
degree-reduction правила). Канонический ответ C2 за 2708 циклов.

**Невыполненная архитектурная развилка** — level reordering не работает,
потому что transform-подзадача «давит в пол» и доходит до max level,
применяя все правила. Хочется комбинировать guarded version правила
на низком уровне + unguarded version на высоком, но для этого нужен
механизм ограничения transform-подзадач (max-level per call, либо
context-aware rule filter, либо block-list propagation). Подумать
архитектурно — отдельный backlog-айтем.

### B2. `blocked_rules` через границу подзадачи (закрыто в task 6)

`subtask(...)` строил fresh `Task` через `TermProps::from(task)`,
теряя `filters.blocked_rules` родительского терма. В task 6 пробрасываем
их в goal-терм подзадачи, чтобы `block(brackets_disclosure)` от
collect rule сохранялся в transform-подзадаче. См. соответствующий
коммит.

### B3. Унификация транзит-правил через `param_solve`

В task 6 для канонизации параметрических ветвей case-split добавлено
семейство transform-правил: обратное к move_left для `==` / `!=`,
swap ориентации `==`, извлечение √ для `a² == c` (две ориентации) и
для `a² ≠ c`, отрицание `-a ≠ -b ⟺ a ≠ b`. Все эти правила —
частные случаи одной идеи: «уточнить значение параметра, решив
соответствующее уравнение».

Концепция: новый процедурный символ `param_solve(eq, p)` — Rust-side
аналог `sympy_solve`, но делегирующий не в SymPy, а в собственный
`Solver` через `solve(Task::find(p, eq))`. Тогда правило типа
`F == 0 && rest <=> param_solve(F == 0, p) && rest` (с подходящими
guards) заменяет всё семейство одним метаправилом и автоматически
покрывает кубику, биквадратные, радикальные — всё, что find умеет.

**Когда делать:** после Phase 1 task 6 (C24 закрыт) — отдельным
рефакторингом. Удалить добавленные частные правила, проверить, что
C1 / C24 остаются зелёными, прогнать на C2 / C26 как обкатку
обобщения.

**Риски:**
- Termination: внутренний `find` может порождать конъюнкции,
  снова матчащие это же правило → рекурсия. Защищаемся
  `MAX_SUBTASK_LEVEL = 10` + структурным guard'ом «не на собственный
  output».
- Performance: каждое срабатывание = sub-solve. Кэш по term-ключу в
  `SolutionState` уже есть, повторных полных прогонов быть не должно.
- Реализация: новый `src/solver/term/symbol/base/param_solve.rs` по
  образцу `substitute.rs`, с доступом к `RulesEngine`. Единственный
  нетривиальный момент — пробросить engine в calculator-контекст.

**Реализация:**
- Шаг 1 — грамматика `find(vars...) { eqs... }` → AST
  `solve(find(vars...), eqs...)`. Отдельный root-символ `solve`
  отделяет sub-solve от обычного `find`-goal'а; multi-var через
  `commasep`.
- Шаг 2 — `resolve_solve_in_hypothesis` в `produce()` запускает
  суб-solver на матче `solve(...) == Param` в requirements,
  splice-ит ответ обратно (без `answer(...)`-обёртки).
- Шаг 3 — **deferred until first usage**. Structural guard на
  рекурсию: правило, делегирующее `solve(find(x), …)` на собственный
  LHS-паттерн, через AC-матчер ловит свой же output →
  MAX_SUBTASK_LEVEL. Лечение известно — блокировать rule_id в
  `blocked_rules` goal'а subtask'а. Срабатывает только при первой
  попытке использовать B3 в реальном правиле; пока такого нет
  (C1/C24/C2 делегируют напрямую `sympy_solve`), guard — спекулятивная
  работа. Реализовывать в момент первого реального usage.
