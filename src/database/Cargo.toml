[package]
name = "database"
version.workspace = true
authors.workspace = true
edition.workspace = true

[lib]
path = "./lib.rs"

[dependencies]
solver = { path = "../solver" }

log = { workspace = true }

bincode = "2.0"
sled = "0.34"
