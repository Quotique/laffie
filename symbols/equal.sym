symbol == {
    attr infix(500),display("=")
}

rule {
    attr replace,level(0),purpose(find(x)),id(move_left);

    a == b <=> a - b == 0;

    !symbolic_eq(b, 0);
}

rule {
    attr replace,level(5),purpose(find(x));

    a + b == c <=> a + b - c == 0;

    !symbolic_eq(c, 0);
}

rule {
    attr level(1),purpose(find(x)),one(a),zero(b),block(move_left);

    x*a + b == 0 => x == - b/a;

    a != 0,
    b is known;
}

rule {
    attr level(5),purpose(find(x)),one(a),zero(b),block(move_left);

    b*a + c == 0 => b == - c/a;

    a != 0,
    c is known;
}
