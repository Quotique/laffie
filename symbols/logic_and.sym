symbol && {
    attr infix(500),display("&"),associative,commutative
};

rule {
    attr subtree,replace;
    
    [a] && [b] <=> [b];

    a is true
};

rule {
    attr subtree,replace;

    [a] && [b] <=> [a];

    a is false
};
