symbol && {
    attr infix(500),display("&"),associative,commutative
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a == c && b <=> b;

    a == c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a != c && b <=> b;

    a != c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a >= c && b <=> b;

    a >= c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a <= c && b <=> b;

    a <= c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a > c && b <=> b;

    a > c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a < c && b <=> b;

    a < c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a == c && b <=> a == c;

    a != c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a != c && b <=> a != c;

    a == c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a <= c && b <=> a <= c;

    a > c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a >= c && b <=> a >= c;

    a < c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a < c && b <=> a < c;

    a >= c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a > c && b <=> a > c;

    a <= c;
}

rule {
    attr level(1),purpose(transform(x)),replace;

    a == b && c =>  a == b && replace(a == b, c);

    a is variable,
    b is known;
}

rule {
    attr level(3),purpose(transform(x)),replace;

    a && (b || c) => (a && b) || (a && c);
}
