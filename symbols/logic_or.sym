symbol || {
    attr infix(600),display("|"),associative,commutative
};

rule {
    attr problem_target(transform),subtree,replace;

    [a] || [b] <=> [a];

    a is true
};

rule {
    attr problem_target(transform),subtree,replace;

    [a] || [b] <=> [b];

    a is false
}
