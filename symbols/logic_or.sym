symbol || {
    attr infix(600),display("|"),associative,commutative
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a == c || b <=> a == c;

    a == c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a != c || b <=> a != c;

    a != c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a <= c || b <=> a <= c;

    a <= c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a >= c || b <=> a >= c;

    a >= c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a < c || b <=> a < c;

    a < c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a > c || b <=> a > c;

    a > c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a == c || b <=> b;

    a != c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a != c || b <=> b;

    a == c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a <= c || b <=> b;

    a > c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a >= c || b <=> b;

    a < c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a < c || b <=> b;

    a >= c;
}

rule {
    attr level(2),purpose(transform(x)),replace;

    a > c || b <=> b;

    a <= c;
}

rule {
    attr level(0),purpose(find(x)),replace;

    x == a || x == b => answer(x in set(a, b));

    a is known,
    b is known;
}

rule {
    attr level(0),purpose(find(x)),replace;

    x == a || x == b || x == c => answer(x in set(a, b, c));

    a is known,
    b is known,
    c is known;
}
