symbol || {
    attr infix(600),display("|"),associative,commutative;
}

rule {
    attr level(2),goal(transform(x)),replace;

    a == c || b <=> a == c;

    a == c;
}

rule {
    attr level(2),goal(transform(x)),replace;

    a != c || b <=> a != c;

    a != c;
}

rule {
    attr level(2),goal(transform(x)),replace;

    a <= c || b <=> a <= c;

    a <= c;
}

rule {
    attr level(2),goal(transform(x)),replace;

    a >= c || b <=> a >= c;

    a >= c;
}

rule {
    attr level(2),goal(transform(x)),replace;

    a < c || b <=> a < c;

    a < c;
}

rule {
    attr level(2),goal(transform(x)),replace;

    a > c || b <=> a > c;

    a > c;
}

rule {
    attr level(2),goal(transform(x)),replace;

    a == c || b <=> b;

    a != c;
}

rule {
    attr level(2),goal(transform(x)),replace;

    a != c || b <=> b;

    a == c;
}

rule {
    attr level(2),goal(transform(x)),replace;

    a <= c || b <=> b;

    a > c;
}

rule {
    attr level(2),goal(transform(x)),replace;

    a >= c || b <=> b;

    a < c;
}

rule {
    attr level(2),goal(transform(x)),replace;

    a < c || b <=> b;

    a >= c;
}

rule {
    attr level(2),goal(transform(x)),replace;

    a > c || b <=> b;

    a <= c;
}

