symbol || {
    attr infix(600),display("|"),associative,commutative
};

rule {
    attr level(0),problem_target(transform),subtree,replace;

    [a] || [b] <=> [a];

    a is true
};

rule {
    attr level(0),problem_target(transform),subtree,replace;

    [a] || [b] <=> [b];

    a is false
}
