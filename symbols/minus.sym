symbol - {
    attr infix(300)
};

rule {
    attr subtree,replace;

    [--a] <=> [a];
};

rule {
    attr subtree,replace;

    [a-b] => [a+(-b)];
};

rule {
    attr problem_target(proof([-a is known]));

    -a is known <=> [true];

    a is known
};
