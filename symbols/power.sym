symbol ^ {
    attr infix(100)
};

rule {
    a * x^2 + b * x + c == 0 => 
        ((b^2 - 4*a*c) < 0 && x in empty_set) || 
        ((b^2 - 4*a*c) >= 0 && x in set( (-b+sqrt(b^2 - 4 * a * c))/(2*a), (-b-sqrt(b^2 - 4 * a * c))/(2*a) ));

    a != 0
}
