symbol set {
    attr commutative;
}

// Нормализация формы множества в case-split-ветвях.
rule {
    attr level(3),goal(transform(x)),replace,block(move_left);

    a in set(b) <=> a == b;
}

rule {
    attr level(3),goal(transform(x)),replace,block(move_left);

    a in set(b, c) <=> a == b || a == c;
}

symbol empty_set {
    attr display("∅ ")
}
