task {
    purpose find(x);
    text "Решить уравнение 2x+5=0";
    answer x == -2.5;

    2*x+5 == 0;
}

task {
    purpose find(x);
    text "Решить уравнение 2x-5=0";
    answer x == 2.5;

    2*x-5 == 0;
}

task {
    purpose find(x);
    text "Решить уравнение 2(x+4)+3(x-5)=0";
    answer x == 1.4;

    2*(x+4) + 3*(x-5) == 0;
}

task {
    purpose find(x);
    text "Решить уравнение 3x+5=1-x";
    answer x == -1;

    3*x+5 == 1-x;
}

