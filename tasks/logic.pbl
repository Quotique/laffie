task {
    purpose transform(1 > 0 && x == 1);
    answer x == 1;

    0;
}

task {
    purpose find(x);
    answer x == 1;

    (1 > 0 && x == 1) || (1 < 0 && x == 2);
}

task {
    purpose find(x);
    answer x == 1;

    x == 1 && x != 0;
}

task {
    purpose find(x);
    answer x in set(4), x == 4;

    (x^4 - 25*x^2 + 60*x -36 != 0) && ((3600 < 0 && x in empty_set) || (3600 >= 0 && x in set(1, 4)));
}

task {
    purpose find(x);
    answer x == 1;

    x == 1 && (sqrt(5) + 2)^2 != 0;
}

task {
    purpose proof(symbolic_eq(0, 0));

    x == 1;
}

task {
    purpose proof(!symbolic_eq(1, 0));

    x == 1;
}

task {
    purpose proof(x == 6/5);

    x in set(6/5, 12/5);
    answer(x in set(6/5, 12/5));
}
