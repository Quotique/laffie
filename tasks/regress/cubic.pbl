task {
    goal find(x);
    text "Решить уравнение 3x^3+2x^2-5x=0";
    answer  x in set((-5)/3, 0, 1), x == 0 || x == (-5)/3 || x == 1, x == 0 || x == 1 || x == -5/3;

    3*x^3 + 2*x^2 -5*x == 0;
}
