task {
    goal find(x);
    text "Решить уравнение -x^2+1 == 0";
    answer x == -1 || x == 1, x == 1 || x == -1;

    -x^2+1 == 0;
}

task {
    goal find(x);

    - 4 - 2*x <= 0 && -x^2 + 4*x == 0 && 2*x + 1 >= 0 && x >= 0;
}

