task {
	goal prove(1 > 0);

	x == 2;
}

task {
	goal prove(-9 < 0);

	x == 2;
}

task {
	goal prove((1>0) is (true));

	x == 2;
}

task {
	goal prove(x > 0);

	x == 2;
}

task {
	goal prove(x > 0);

	x == 2 + 2;
}

task {
    goal find(x);
    answer x == 1;

    x == 1;
}

task {
    goal find(x);
    answer x == 1, x in set(1);

    x in set(1);
}

task {
    goal find(x);
    answer x == 1 || x == (sqrt(2) + 5)/2, x in set(1, (sqrt(2) + 5)/2 ), x == (sqrt(2) + 5)/2 || x == 1;

    x in set(1, (sqrt(2) + 5)/2 );
}

task {
    goal find(x);
    answer x == 1;

    -(-x) == 1;
}

task {
    goal find(x);
    answer x == -3;

    x+3 == 0;
}

