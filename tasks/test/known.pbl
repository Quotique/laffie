task {
    purpose proof(set(5) is known);

    x == 1;
}

task {
    purpose proof(set(5, 1) is known);

    x == 1;
}

task {
    purpose proof(set(5, 1, 2) is known);

    x == 1;
}

task {
    purpose proof( ((-sqrt(2944)+10)/(-18)) is known );

    x == 1;
}
