task {
    goal find(x);
    text "Решить уравнение sqrt(5*x - 3) - sqrt(2*x - 1) == sqrt(3*x - 2)"; 

    sqrt(5*x - 3) - sqrt(2*x - 1) == sqrt(3*x - 2);
}
task {
    goal find(x);
    text "Решить уравнение sqrt( (2*sqrt(7) + sqrt(2) - 3*sqrt(5))*x ) == x";

    sqrt( (2*sqrt(7) + sqrt(2) - 3*sqrt(5))*x ) == x;
}
task {
    goal find(x);
    text "Решить уравнение sqrt(5-x) == sqrt(3) + 2 - x";

    sqrt(5-x) == sqrt(3) + 2 - x;
}
task {
    goal find(x);
    text "Решить уравнение sqrt(x^2-7*x+1) == sqrt(2*x^2-15*x+8)";

    sqrt(x^2-7*x+1) == sqrt(2*x^2-15*x+8);
}
task {
    goal find(x);
    text "Решить уравнение sqrt(x + 1) * sqrt(2*x+3) == x+3";

    sqrt(x + 1) * sqrt(2*x+3) == x+3;
}
task {
    goal find(x);
    text "Решить уравнение sqrt(3*x + 1) - sqrt(2*x-1) == 7";

    sqrt(3*x + 1) - sqrt(2*x-1) == 7;
}
task {
    goal find(x);
    text "Решить уравнение sqrt(5-x) + sqrt(8+2*x) == 2";

    sqrt(5-x) + sqrt(8+2*x) == 2;
}
task {
    goal find(x);
    text "Решить уравнение sqrt(x^2+x+1) + sqrt(x^2+(3/4)*x) == sqrt(4*x^2 + 3*x)";

    sqrt(x^2+x+1) + sqrt(x^2+(3/4)*x) == sqrt(4*x^2 + 3*x);
}
task {
    goal find(x);
    text "Решить уравнение 2*x - sqrt(x^3 + 2*x^2 - 3*x) == 0";

    2*x - sqrt(x^3 + 2*x^2 - 3*x) == 0;
}
task {
    goal find(x);
    text "Решить уравнение x + sqrt( (x^2 + 4*x)/(x-2) ) == 0";

    x + sqrt( (x^2 + 4*x)/(x-2) ) == 0;
}
task {
    goal find(x);
    text "Решить уравнение (1-x)/(1+x) * sqrt( (1+3*x)/(1-3*x) ) == 0";

    (1-x)/(1+x) * sqrt( (1+3*x)/(1-3*x) ) == 0;
}
task {
    goal find(x);
    text "Решить уравнение sqrt(4+x) / (2 + sqrt(4+x)) == sqrt(4-x) / (2- sqrt(4-x))";

    sqrt(4+x) / (2 + sqrt(4+x)) == sqrt(4-x) / (2 - sqrt(4-x));
}
task {
    goal find(x);
    text "Решить уравнение (sqrt(x+6) - sqrt(x-6))/(sqrt(x+6) + sqrt(x-6)) == x / 6";

    (sqrt(x+6) - sqrt(x-6))/(sqrt(x+6) + sqrt(x-6)) == x / 6;
}
task {
    goal find(x);
    text "Решить уравнение sqrt(5*x + 7) - sqrt(x + 4) == 4*x + 3";

    sqrt(5*x + 7) - sqrt(x + 4) == 4*x + 3;
}
task {
    goal find(x);
    text "Решить уравнение sqrt(45*x + 12) - sqrt(15*x + 2) == sqrt(10)*(3*x+1)";

    sqrt(45*x + 12) - sqrt(15*x + 2) == sqrt(10)*(3*x+1);
}
task {
    goal find(x);
    text "Решить уравнение sqrt( 2*x + 3 ) + sqrt( 3*x - 1 ) = sqrt( 5*x + 2 )";

    sqrt( 2*x + 3 ) + sqrt( 3*x - 1 ) == sqrt( 5*x + 2 );
}
task {
    goal find(x);
    text "Решить уравнение sqrt( x^2 - 5*x + 1 ) + sqrt( 8*x - x^2 - 12 ) == sqrt( 3*x - 11 )";

    sqrt( x^2 - 5*x + 1 ) + sqrt( 8*x - x^2 - 12 ) == sqrt( 3*x - 11 );
}
// task {
//     goal find(x);
//     text "Решить уравнение sqrt(3, 2*x + 3) + sqrt(3, 3*x - 1) == sqrt(3, 5*x + 2)";
// 
//     sqrt(3, 2*x + 3) + sqrt(3, 3*x - 1) == sqrt(3, 5*x + 2);
// }

// task {
//     goal find(x);
//     text "Решить уравнение sqrt(3, x^2 - 7*x + 10) + sqrt(3, x^2 - 9*x - 36) == sqrt(3, 2*x^2 - 16*x - 26)";
// 
//     sqrt(3, x^2 - 7*x + 10) + sqrt(3, x^2 - 9*x - 36) == sqrt(3, 2*x^2 - 16*x - 26);
// }

task {
    goal find(x);
    text "Решить уравнение sqrt( x^2 + 3*x - 2 ) + sqrt( x^2 - x + 1 ) == 4*x - 3";

    sqrt( x^2 + 3*x - 2 ) + sqrt( x^2 - x + 1 ) == 4*x - 3;
}
task {
    goal find(x);
    text "Решить уравнение sqrt(2 + sqrt(6) - (6*sqrt(2) -  2*sqrt(3))*x) == 2*x - sqrt(2)";

    sqrt(2 + sqrt(6) - (6*sqrt(2) - 2*sqrt(3))*x) == 2*x - sqrt(2);
}
task {
    goal find(x);
    text "Решить уравнение sqrt(x^2 - 5*x + 2) - sqrt(x^2 + x + 1) == 1 - 6*x";

    sqrt(x^2 - 5*x + 2) - sqrt(x^2 + x + 1) == 1 - 6*x;
}
task {
    goal find(x);
    text "Решить уравнение 2 * ( sqrt(x + 15) - sqrt(x) ) == 3 * ( sqrt(x + 3) - sqrt(x - 1) )";

    2 * (sqrt(x + 15) - sqrt(x)) == 3 * ( sqrt(x + 3) - sqrt(x - 1) );
}
task {
    goal find(x);
    text "Решить уравнение sqrt(9*x^2 - 12*x + 11) - sqrt(5*x^2 - 8*x + 10) == 2*x - 1";

    sqrt(9*x^2 - 12*x + 11) - sqrt(5*x^2 - 8*x + 10) == 2*x - 1;
}
// task {
//     goal find(x);
//     text "Решить уравнение sqrt(7, 2 + x)/x + sqrt(7, 2 + x)/2 == sqrt(7, x)/3";
// 
//     sqrt(7, 2 + x)/x + sqrt(7, 2 + x)/2 == sqrt(7, x)/3; 
// }

// task {
//     goal find(x);
//     text "Решить уравнение sqrt(5, 5 - x)/(x^2) - sqrt(5, 5 - x)/25 == sqrt(5, (x^2)/(x+5))";
// 
//     sqrt(5, 5 - x)/(x^2) - sqrt(5, 5 - x)/25 == sqrt(5, (x^2)/(x+5)); 
// }

task {
    goal find(x);
    text "Решить уравнение (x^2 + 1)/(3*x^2 + 2) == (4*x^2 - 5)/(x^2 + 6)";

    (x^2 + 1)/(3*x^2 + 2) == (4*x^2 - 5)/(x^2 + 6);
}
task {
    goal find(x);
    text "Решить уравнение (3*x^2)/(x+1) + (x+1)/(x^2) == 4";

    (3*x^2)/(x+1) + (x+1)/(x^2) == 4;
}
task {
    goal find(x);
    text "Решить уравнение 5*sqrt(x+1) * sqrt(x+3) == 2*sqrt(2)*(x^2 + 4*x)";

    5*sqrt(x+1) * sqrt(x+3) == 2*sqrt(2)*(x^2 + 4*x);
}
// task {
//     goal find(x);
//     text "Решить уравнение 2*sqrt(3, x) + sqrt(6, x) == 10";
// 
//     2*sqrt(3, x) + sqrt(6, x) == 10;
// }

// Task {
//     goal find(x);
//     text "Решить уравнение x^(20/21) + x^(5/42) == 12 * x^(- 5/7)";
// 
//     x^(20/21) + x^(5/42) == 12 * x^(- 5/7);
// }

task {
    goal find(x);
    text "Решить уравнение sqrt(2 - x) + 4/(sqrt(2 - x) + 3) == 2";

    sqrt(2 - x) + 4/(sqrt(2 - x) + 3) == 2;
}
task {
    goal find(x);
    text "Решить уравнение 1/(sqrt(x+1) - 2) == 1 / sqrt(x+1) + 2/3";

    1/(sqrt(x+1) - 2) == 1 / sqrt(x+1) + 2/3;
}
task {
    goal find(x);
    text "Решить уравнение sqrt( x^2 + x ) + sqrt( x^2 + x + 5 ) == sqrt( 2*x^2 + 2*x + 17 )";

    sqrt( x^2 + x ) + sqrt( x^2 + x + 5 ) == sqrt( 2*x^2 + 2*x + 17 );
}
task {
    goal find(x);
    text "Решить уравнение sqrt(2 + x / (sqrt(x) + 1)) - 1 == sqrt(3 - x/(sqrt(x) + 1))";

    sqrt(2 + x / (sqrt(x) + 1)) - 1 == sqrt(3 - x/(sqrt(x) + 1));
}
// task {
//     goal find(x);
//     text "Решить уравнение sqrt(2 + x / (sqrt(x) + 1)) - 1 == sqrt(3 - x/(sqrt(x) + 1))";
// 
//     sqrt(3, 1 + sqrt(x)) == sqrt(1 + sqrt(3, x));
// }


task {
    goal find(x);
    text "Решить уравнение 1/x + 1/sqrt(1-x^2) == 35/12";

    1/x + 1/sqrt(1-x^2) == 35/12;
}
